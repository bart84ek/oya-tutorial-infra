Project: TutInfra

Changeset: |
  echo "+staging/"

onPush: |
  oya run --changest -r deploy

onMergeToMaster: |
  oya run --changest -r deploy
  # git diff tag:last_realse

onMergeToProd: |
  oya run --changest -r deploy
